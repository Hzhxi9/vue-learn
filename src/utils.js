/**
 * 将key 代理到target上
 * 比如 代理 this._data.xx 为 this.xx
 * @param {*} target 目标对象, 比如vm
 * @param {*} source source 原始key, 比如_data
 * @param {*} key 代理的原始对象上的指定属性， 比如_data.xx
 */

export function proxy(target, source, key) {}
